<resources>
    <!-- Only what shows before Compose draws: the window background. AppCompat, as
         MainActivity is an AppCompatActivity for the per-app language. -->
    <style name="Theme.Godlo" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/seed</item>
    </style>
</resources>
