<resources>
    <!-- Only what shows before Compose draws: the window background. -->
    <style name="Theme.Godlo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/seed</item>
    </style>
</resources>
